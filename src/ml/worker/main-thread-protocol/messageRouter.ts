import "../legacyWorker";

// export const mountMessageRouter = (_workerScope: DedicatedWorkerGlobalScope): void => {
export const mountMessageRouter = (): void => {
  // Router is currently mounted by legacy worker side effects.
};
